// import {RolUsuario} from './data_user'

// export const getSessionRolFromLocalStorage = (): number | null => {
//     const sessionRolString:string | null = localStorage.getItem('sessionRol');
//     const rol = sessionRolString ? (RolUsuario[...sessionRolString]) : (null);
//     if (rol) {
//       return (rol);
//     }
//     return null;
//   };
export {}
